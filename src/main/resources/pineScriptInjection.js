/*var plotTimeScript =
    '<div class="ace_line" style="height:15px">' +
    '<span class="ace_support ace_function">plot</span>' +
    '<span class="ace_paren">(</span>' +
    '<span class="ace_support ace_variable">time</span>' +
    '<span class="ace_paren">)</span>' +
    '</div>';

// We have access to jQuery in TradingView, so use it.

// Remove the default line 2 of the study to replace it
$('div.ace_content .ace_layer.ace_text-layer .ace_line:nth-child(2)').remove();

// Add the new line of the study: 'plot(time)'
$('div.ace_content .ace_layer.ace_text-layer').append(plotTimeScript);*/

//$('textarea.ace_text-input').style()